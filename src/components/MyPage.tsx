import ExploreContainer from './ExploreContainer';


interface ContainerProps {
  name: string;
}

const MyPage: React.FC<ContainerProps> = ({ name }) => {
    return(
        <div>mypage!!!</div>
    )
};

export default MyPage;
